GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
MCP_API_KEY=            # Required for HTTP mode (--http). Use: openssl rand -hex 32
HOST=127.0.0.1
PORT=3000
# PUBLIC_HOST=your-domain.com  # Optional: for reverse proxy DNS-rebinding protection (Rust only)
