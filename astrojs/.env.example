# OAuth2 client from Google Cloud (same as the Rust MCP server)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: if not set, reads refresh_token from ../health_token.json
GOOGLE_REFRESH_TOKEN=

# Optional: path to token file (default: ../health_token.json)
# GH_TOKEN_FILE=../health_token.json
